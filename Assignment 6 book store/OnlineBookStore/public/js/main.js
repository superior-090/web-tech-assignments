(() => { const params=new URLSearchParams(location.search), message=params.get("error")||params.get("success"); document.querySelectorAll(".flash-message").forEach(box=>{if(message){box.textContent=message;box.classList.add(params.has("error")?"error":"success");}}); const toggle=document.querySelector(".menu-toggle"); if(toggle)toggle.addEventListener("click",()=>document.querySelector(".main-nav").classList.toggle("open")); fetch("/api/session").then(res=>res.json()).then(async ({loggedIn,user})=>{if(!loggedIn)return;document.querySelectorAll(".guest-nav").forEach(el=>el.hidden=true);document.querySelectorAll(".user-nav").forEach(el=>{el.hidden=false;el.querySelector(".welcome").textContent=`Hi, ${user.name.split(" ")[0]}`;if(!el.querySelector(".cart-link")){const link=document.createElement("a");link.href="/cart";link.className="cart-link";link.innerHTML='Cart <span class="cart-count">0</span>';el.prepend(link);}}); const cart=await fetch("/api/cart").then(r=>r.ok?r.json():[]); const count=cart.reduce((sum,item)=>sum+item.quantity,0); document.querySelectorAll(".cart-count").forEach(el=>el.textContent=count);}).catch(()=>{}); })();
